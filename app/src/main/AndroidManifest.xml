<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.thangvnnc.appmap">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".application.BaseApplication"
        android:theme="@style/Theme.AppMap">

        <activity
            android:name="net.thangvnnc.appmap.ui.stores.directions.DirectionsActivity"
            android:label="@string/title_activity_directions" />
        <activity
            android:name="net.thangvnnc.appmap.ui.stores.locations.LocationsActivity"
            android:label="@string/title_activity_locations" />

        <activity
            android:name="net.thangvnnc.appmap.ui.stores.locations.LocationDetailsActivity"
            android:label="@string/title_activity_location_detail_txt_name" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name="net.thangvnnc.appmap.ui.main.MainBaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="net.thangvnnc.appmap.service.GPSService"
            android:process="net.thangvnnc.appmap.service.gps" />
    </application>

</manifest>